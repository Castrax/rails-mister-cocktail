<h1><%= @cocktail.name %></h1>

<ul>
  <% @doses.each do |dose| %>
   <li>Please add: <%= dose.description %> <em><strong><%= dose.ingredient.name %></em></strong><%= link_to '🗑', cocktail_dose_path(dose), method: :delete, data: { confirm: "U sure u want to delete this dose?" }  %></li>
  <% end %>
</ul>

<p><%= link_to "Add a dose", new_cocktail_dose_path(@cocktail) %></p>
<p><%= link_to "Back to the cocktails", cocktails_path %></p>
